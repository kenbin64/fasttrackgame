{
  "name": "fasttrack-lobby-server",
  "version": "0.1.0",
  "description": "Simple WebSocket lobby server for FastTrack",
  "main": "server/ws_lobby_server.js",
  "scripts": {
    "start-lobby": "node server/ws_lobby_server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
